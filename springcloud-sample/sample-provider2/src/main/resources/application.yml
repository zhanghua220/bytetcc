server:
  port: 7071
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:7000/eureka/
hystrix:
 metrics:
  enabled: true
 command:
  default:
   execution:
    isolation:
     thread:
       timeoutInMilliseconds: 100000